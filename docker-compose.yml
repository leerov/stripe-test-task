services:
  web:
    build: .
    command: sh -c "python manage.py migrate && python manage.py create_default_superuser && python manage.py runserver 0.0.0.0:8000"
    ports:
      - "9290:8000"
    env_file:
      - .env
    image: leerov_stripe-test-task_web
